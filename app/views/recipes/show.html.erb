<%= form_with(url: cook_recipe_path(@recipe), method: :post, local: true) do |form| %>
  <%= form.submit "作る", class: "cook-button" %>   
  <%= link_to "作った！", made_recipe_path(@recipe), method: :post, class: "btn btn-primary" %>
<% end %>